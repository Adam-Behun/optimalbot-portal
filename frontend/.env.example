# OptimalBot Portal Frontend - Environment Variables
# Copy this file to .env.local for local development overrides
#
# Vite automatically loads:
#   - .env.development (local dev)
#   - .env.production (production build)
#   - .env.local (local overrides, gitignored)

# API Base URL
# - Leave empty to use Vite's proxy (recommended for local dev)
# - Set to backend URL for direct API calls
# Examples:
#   Local dev with proxy: (leave empty or don't set)
#   Local dev direct:     http://localhost:8000
#   Production:           https://api.optimalbot.ai
VITE_API_BASE_URL=
