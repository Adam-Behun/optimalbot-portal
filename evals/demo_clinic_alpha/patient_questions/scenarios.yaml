# Adversarial scenarios for patient questions flow evaluation
# Each scenario targets specific weaknesses in flow_definition.py
#
# Usage:
#   python run.py --scenario <id>
#   python run.py --all
#   python run.py --sync-dataset  # Push scenarios to Langfuse dataset

dataset_name: "demo_clinic_alpha/patient_questions"
dataset_description: "Adversarial scenarios for patient questions flow testing"

scenarios: []
