# Core pipecat with required providers (includes openai, daily-python, opentelemetry packages)
pipecat-ai[daily,cartesia,elevenlabs,deepgram,groq,tracing]
pipecat-ai-flows

# Duplicates - already included in pipecat-ai[tracing]
# openai
# daily-python
# opentelemetry-api
# opentelemetry-sdk
# opentelemetry-exporter-otlp-proto-http

# Required by pipecat even if not used (imported at module level)
anthropic

# Database
# pymongo  # Included with motor
motor

# Environment variables (not needed - use os.environ directly)
# python-dotenv

# Additional dependencies
langfuse
pyyaml  # For services.yaml parsing
pydantic  # For backend API schemas
tenacity
phonenumbers